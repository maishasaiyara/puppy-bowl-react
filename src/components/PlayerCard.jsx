import React from 'react';
import { fetchAllPlayers } from '../api/ajaxHelpers';

const PlayerCard = () => {
      
            return(
        <>
        <div> Each player card goes here </div>
        </>
            );
};

export default PlayerCard;